{{- if .Values.basicAuth.enabled }}
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ include "backend.fullname" . }}-basic-auth
  labels:
    {{- include "backend.labels" . | nindent 4 }}
spec:
  basicAuth:
    secret: {{ .Values.basicAuth.secretName }}
{{- end }}
